# GF-Email-Typo-Guard
Adds a per-field "domain typo suggestion" option to Gravity Forms Email fields (e.g. suggests "gmail.com" when a user types "gamil.con"). Scripts and styles are only loaded on pages that actually render a form using the feature.
